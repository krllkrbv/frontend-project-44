#!/usr/bin/env node
    import { userName, randomNumber, answer } from "../src/even.js";
    console.log("Welcome to the Brain Games!");
    const name = userName();
    console.log('Answer "yes" if the number is even, otherwise answer "no".');
    answer(name);
